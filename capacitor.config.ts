import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.agustin.cuidaitoconeso',
  appName: 'Cuidaito Con Eso',
  webDir: 'out'
};

export default config;
