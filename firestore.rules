
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Helper Functions
    function isSignedIn() {
      return request.auth != null;
    }
    
    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }
    
    // Rules for user profiles
    match /users/{userId} {
      allow read, write: if isOwner(userId);
    }

    // Rules for game documents
    match /games/{gameId} {
      // Any signed-in user can read game data
      allow read: if isSignedIn();
      // Only players in the game can write to it
      allow write: if isSignedIn() && request.auth.uid in resource.data.playerIds;
    }

    // Rules for card images, accessible by any authenticated user (admin panel is protected)
    match /card-images/{cardId} {
        allow read, write: if isSignedIn();
    }

    // Rules for music files, accessible by any authenticated user (admin panel is protected)
    match /music/{fileName} {
        allow read, write: if isSignedIn();
    }
  }
}
